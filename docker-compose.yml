services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      # ソースコードをホストとコンテナで同期（ホットリロード対応）
      - .:/app
      # node_modules はコンテナ側を使用（ホストのものを上書きしない）
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
