import { Sound } from "narraleaf-react";

export const chapterTitleSound = new Sound({
  src: "/audio/effect/chapter_title_sound.mp3",
  loop: false,
  volume: 0.5,
});

export const menuSelectSound = new Sound({
  src: "/audio/effect/menu_select_sound.mp3",
  loop: false,
  volume: 0.5,
});